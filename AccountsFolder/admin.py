"""
Admin configuration for the AccountsFolder application.

This module uses Django's default admin configuration for the
built-in User model. No custom admin classes are needed as user
management is handled by django.contrib.auth.admin.
"""

from django.contrib import admin

# User admin is automatically registered by django.contrib.auth.
# No additional admin configuration is needed for this app.
